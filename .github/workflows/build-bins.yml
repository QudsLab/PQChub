name: Build Binariesname: Build Binaries



on:on:

  workflow_dispatch:  # Manual trigger only

    inputs:  workflow_dispatch:

      pqclean_ref:    inputs:

        description: 'PQClean branch/tag/commit to build'      pqclean_ref:

        required: false        description: 'PQClean branch/tag/commit to build'

        default: 'master'        required: false

        type: string        default: 'master'

        type: string

env:

  PQCLEAN_REF: ${{ github.event.inputs.pqclean_ref || 'master' }}env:

  PQCLEAN_REF: ${{ github.event.inputs.pqclean_ref || 'master' }}

jobs:

  build_native:jobs:

    name: Build (${{ matrix.target }})  build_native:

    runs-on: ${{ matrix.runner }}    name: Build Native (${{ matrix.os }}-${{ matrix.arch }})

    strategy:    runs-on: ${{ matrix.runner }}

      fail-fast: false    strategy:

      matrix:      fail-fast: false

        include:      matrix:

          - os: linux        include:

            arch: x86_64          # Linux builds

            runner: ubuntu-latest          - os: linux

            target: linux-x86_64            arch: x86_64

          - os: macos            runner: ubuntu-latest

            arch: arm64            target: linux-x86_64

            runner: macos-latest            cross: false

            target: macos-arm64          - os: linux

          - os: windows            arch: aarch64

            arch: x64            runner: ubuntu-latest

            runner: windows-latest            target: linux-aarch64

            target: windows-x64            cross: true

          

    steps:          # macOS builds

    - uses: actions/checkout@v4          - os: macos

                arch: x86_64

    - uses: actions/setup-python@v4            runner: macos-15-intel  # Intel runner

      with:            target: macos-x86_64

        python-version: '3.11'            cross: false

          - os: macos

    - name: Setup Linux            arch: arm64

      if: matrix.os == 'linux'            runner: macos-latest  # Apple Silicon runner

      run: sudo apt-get update && sudo apt-get install -y build-essential            target: macos-arm64

            cross: false

    - name: Setup Windows          

      if: matrix.os == 'windows'          # Windows builds

      uses: ilammy/msvc-dev-cmd@v1          - os: windows

      with:            arch: x64

        arch: ${{ matrix.arch }}            runner: windows-latest

            target: windows-x64

    - name: Clone PQClean            cross: false

      run: git clone --depth 1 --branch ${{ env.PQCLEAN_REF }} https://github.com/PQClean/PQClean.git pqclean-source          - os: windows

            arch: x86

    - name: Build (Unix)            runner: windows-latest

      if: matrix.os != 'windows'            target: windows-x86

      run: |            cross: true

        chmod +x scripts/build_native.sh

        ./scripts/build_native.sh ${{ matrix.target }} pqclean-source    steps:

    - name: Checkout repository

    - name: Build (Windows)      uses: actions/checkout@v4

      if: matrix.os == 'windows'

      run: python scripts/build_native.py ${{ matrix.target }} pqclean-source    - name: Setup Python

      uses: actions/setup-python@v4

    - name: Upload      with:

      uses: actions/upload-artifact@v4        python-version: '3.11'

      with:

        name: ${{ matrix.target }}    # Linux-specific setup

        path: bins/${{ matrix.target }}/    - name: Setup Linux build tools

      if: matrix.os == 'linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential
        if [ "${{ matrix.cross }}" = "true" ]; then
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
        fi

    # Windows-specific setup
    - name: Setup Windows MSVC
      if: matrix.os == 'windows'
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: ${{ matrix.arch }}

    # Clone PQClean source
    - name: Clone PQClean
      run: git clone --depth 1 --branch ${{ env.PQCLEAN_REF }} https://github.com/PQClean/PQClean.git pqclean-source

    # Build native library
    - name: Build (Unix)
      if: matrix.os != 'windows'
      run: |
        chmod +x scripts/build_native.sh
        ./scripts/build_native.sh ${{ matrix.target }} pqclean-source
      env:
        CC: ${{ matrix.cross && matrix.arch == 'aarch64' && 'aarch64-linux-gnu-gcc' || 'gcc' }}
        CXX: ${{ matrix.cross && matrix.arch == 'aarch64' && 'aarch64-linux-gnu-g++' || 'g++' }}

    - name: Build (Windows)
      if: matrix.os == 'windows'
      run: python scripts/build_native.py ${{ matrix.target }} pqclean-source

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: binaries-${{ matrix.target }}
        path: bins/${{ matrix.target }}/
        retention-days: 7

  build_android:
    name: Build Android (${{ matrix.abi }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        abi:
          - arm64-v8a
          - armeabi-v7a
          - x86_64
          - x86

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Setup Android NDK
      id: setup-ndk
      uses: nttld/setup-ndk@v1
      with:
        ndk-version: r25c

    - name: Clone PQClean
      run: git clone --depth 1 --branch ${{ env.PQCLEAN_REF }} https://github.com/PQClean/PQClean.git pqclean-source

    - name: Build Android library
      run: |
        chmod +x scripts/build_android.sh
        ./scripts/build_android.sh ${{ matrix.abi }} pqclean-source
      env:
        ANDROID_NDK_ROOT: ${{ steps.setup-ndk.outputs.ndk-path }}

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: binaries-android-${{ matrix.abi }}
        path: bins/android-${{ matrix.abi }}/
        retention-days: 7
      uses: actions/download-artifact@v4
      with:
        path: downloaded-artifacts

    - name: Organize binaries
      run: |
        # Create bins directory structure
        mkdir -p bins
        
        # Move all downloaded artifacts to bins/
        for artifact_dir in downloaded-artifacts/binaries-*; do
          if [ -d "$artifact_dir" ]; then
            target_name=$(basename "$artifact_dir" | sed 's/^binaries-//')
            echo "Processing $target_name"
            mkdir -p "bins/$target_name"
            cp -r "$artifact_dir"/* "bins/$target_name/"
          fi
        done
        
        # Create global build info
        cat > bins/BUILD_INFO.txt << EOF
        PQChub Binary Build Information
        ===============================
        
        Build Date: $(date -u)
        PQClean Reference: ${{ env.PQCLEAN_REF }}
        GitHub Run ID: ${{ github.run_id }}
        GitHub Run Number: ${{ github.run_number }}
        Triggered by: ${{ github.event_name }}
        
        Successfully built platforms:
        $(find bins -name "*.so" -o -name "*.dylib" -o -name "*.dll" | sed 's|bins/||g' | sort)
        
        Algorithms included:
        - Kyber512, Kyber768, Kyber1024 (Key Encapsulation)
        - Dilithium2, Dilithium3, Dilithium5 (Digital Signatures)
        - Falcon-512, Falcon-1024 (Digital Signatures)
        
        Total binary size: $(du -sh bins | cut -f1)
        
        Usage:
        Clone this repository and use the language wrappers in wrappers/
        to access these pre-compiled binaries.
        EOF
        
        # Create binary listing
        cat > bins/BINARIES.txt << EOF
        PQChub Binary Listing
        =====================
        
        $(find bins -type f \( -name "*.so" -o -name "*.dylib" -o -name "*.dll" \) -exec ls -lh {} \; | sort)
        EOF

    - name: Configure Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

    - name: Check for changes
      id: check_changes
      run: |
        git add bins/
        if git diff --staged --quiet; then
          echo "No changes to commit"
          echo "changes=false" >> $GITHUB_OUTPUT
        else
          echo "Changes detected"
          echo "changes=true" >> $GITHUB_OUTPUT
        fi

    - name: Commit and push binaries
      if: steps.check_changes.outputs.changes == 'true'
      run: |
        git commit -m "🤖 Auto-update binaries from PQClean ${{ env.PQCLEAN_REF }} [skip ci]

        - Build date: $(date -u)
        - GitHub run: ${{ github.run_id }}
        - Trigger: ${{ github.event_name }}
        - Platforms: $(find bins -name "*.so" -o -name "*.dylib" -o -name "*.dll" | wc -l) binaries
        "
        git push

    - name: Create build summary
      run: |
        echo "## 🚀 Binary Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Platform | Status | Binary Size |" >> $GITHUB_STEP_SUMMARY
        echo "|----------|--------|-------------|" >> $GITHUB_STEP_SUMMARY
        
        for platform_dir in bins/*/; do
          if [ -d "$platform_dir" ]; then
            platform=$(basename "$platform_dir")
            binary_file=$(find "$platform_dir" -name "*.so" -o -name "*.dylib" -o -name "*.dll" | head -1)
            if [ -f "$binary_file" ]; then
              size=$(ls -lh "$binary_file" | awk '{print $5}')
              echo "| $platform | ✅ Built | $size |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| $platform | ❌ Failed | - |" >> $GITHUB_STEP_SUMMARY
            fi
          fi
        done
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Total binaries**: $(find bins -name "*.so" -o -name "*.dylib" -o -name "*.dll" | wc -l)" >> $GITHUB_STEP_SUMMARY
        echo "**Total size**: $(du -sh bins | cut -f1)" >> $GITHUB_STEP_SUMMARY
        echo "**PQClean ref**: ${{ env.PQCLEAN_REF }}" >> $GITHUB_STEP_SUMMARY

  test_wrappers:
    name: Test Language Wrappers
    needs: commit_binaries
    runs-on: ${{ matrix.os }}
    if: needs.commit_binaries.result == 'success'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        language: [python, nodejs]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        lfs: true

    - name: Pull latest changes
      run: git pull

    - name: Setup Python
      if: matrix.language == 'python'
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Setup Node.js
      if: matrix.language == 'nodejs'
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Test Python wrapper
      if: matrix.language == 'python'
      run: |
        cd wrappers/python
        pip install -e .
        python -c "
        try:
            from pqchub import Kyber512
            kyber = Kyber512()
            pk, sk = kyber.keypair()
            ct, ss = kyber.encapsulate(pk)
            ss2 = kyber.decapsulate(ct, sk)
            assert ss == ss2
            print('✅ Python wrapper test passed')
        except Exception as e:
            print(f'❌ Python wrapper test failed: {e}')
            exit(1)
        "

    - name: Test Node.js wrapper
      if: matrix.language == 'nodejs'
      run: |
        cd wrappers/nodejs
        npm install
        node -e "
        try {
            const { Kyber512 } = require('./index.js');
            const kyber = new Kyber512();
            const { publicKey, secretKey } = kyber.keypair();
            const { ciphertext, sharedSecret } = kyber.encapsulate(publicKey);
            const decryptedSecret = kyber.decapsulate(ciphertext, secretKey);
            if (Buffer.compare(sharedSecret, decryptedSecret) === 0) {
                console.log('✅ Node.js wrapper test passed');
            } else {
                throw new Error('Shared secrets do not match');
            }
        } catch (e) {
            console.log('❌ Node.js wrapper test failed:', e.message);
            process.exit(1);
        }
        "